<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Thank You</title>
    <style>
      body,h1,p,a {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      a {
        text-decoration: none;
      }

      /* General styling */
      body {
        font-family: Arial, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background: #f0f8ff;
        margin: 0;
      }

      /* Container styling */
      .thankyou-container {
        text-align: center;
        background: #ffffff;
        padding: 40px 20px;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        width: 90%;
        max-width: 500px;
      }

      /* Heading styling */
      .thankyou-container h1 {
        color: #4caf50;
        font-size: 2.5em;
        margin-bottom: 20px;
      }

      /* Paragraph styling */
      .thankyou-container p {
        color: #555555;
        font-size: 1.2em;
        margin-bottom: 20px;
      }

      /* Button styling */
      .thankyou-container .button {
        display: inline-block;
        background: #4caf50;
        color: #ffffff;
        text-decoration: none;
        font-size: 1em;
        padding: 10px 20px;
        border-radius: 5px;
        transition: background 0.3s ease;
      }

      .thankyou-container .button:hover {
        background: #45a049;
      }
    </style>
  </head>
  <body>
    <div class="thankyou-container">
      <h1>Thank You!</h1>
      <p>Your test has been successfully submitted.</p>
      <p>We appreciate your effort and will review your answers shortly.</p>
      <a href="/edtech" class="button">Go to Homepage</a>
    </div>
  </body>
</html>